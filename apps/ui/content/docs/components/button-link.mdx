---
title: ButtonLink
description: A link component that looks like a button, designed for navigation.
---

<ComponentPreview name="p-button-17" />

## Installation

<CodeTabs>

<TabsList>
  <TabsTab value="cli">CLI</TabsTab>
  <TabsTab value="manual">Manual</TabsTab>
</TabsList>
<TabsPanel value="cli">

```bash
npx shadcn@latest add @coss/button-link
```

</TabsPanel>

<TabsPanel value="manual">

<Steps>

<Step>Install the following dependencies:</Step>

```bash
npm install @base-ui-components/react
```

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource name="button-link" title="components/ui/button-link.tsx" />

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsPanel>

</CodeTabs>

## Usage

```tsx
import { ButtonLink } from "@/components/ui/button-link"
```

```tsx
<ButtonLink href="/about">About</ButtonLink>
```

## Overview

`ButtonLink` renders links that look like buttons. It uses the same visual styling as `Button` but renders an `<a>` element by default, ensuring correct semantics and behavior for navigation.

Buttons and links have fundamentally different behaviors:
- `<a>` triggers via `Enter`, `<button>` triggers via `Space`
- `Space` scrolls the page when focused on `<a>`, but triggers action on `<button>`
- Different semantic meaning, context menus, and screen reader announcements

`ButtonLink` ensures navigation elements use the correct semantic element while maintaining button-like styling.

### When to Use

| Use `ButtonLink` when | Use `Button` when |
| :------------------- | :---------------- |
| Building navigation elements | Triggering actions |
| Need proper link semantics (`<a>`) | Form submission |
| Links that look like buttons | Click handlers |

## API Reference

`ButtonLink` supports the same `variant` and `size` props as `Button`, plus standard anchor (`<a>`) element props.

| Prop        | Type                                                          | Default   | Description                                    |
| :---------- | :------------------------------------------------------------ | :-------- | :--------------------------------------------- |
| `href`      | `string`                                                      | -         | The URL the link points to                     |
| `variant`   | `"default" \| "destructive" \| "outline" \| "ghost" \| ...` | `"default"` | The visual style variant                       |
| `size`      | `"xs" \| "sm" \| "default" \| "lg" \| "xl" \| "icon" \| ...` | `"default"` | The size of the button                         |
| `render`    | `ReactElement \| ((props) => ReactElement)`                   | -         | Custom element to render (e.g., Next.js `Link`) |
| `className` | `string`                                                      | -         | Additional CSS classes                          |

All standard anchor (`<a>`) element props are also supported. By default, `ButtonLink` renders an `<a>` element. Use the `render` prop to render a different component like Next.js `Link`.

